<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aashishkumar.rootcause01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TrackTrip"
            android:label="@string/title_activity_track_trip"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ViewPastTrips"
            android:label="@string/title_activity_view_past_trips"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsScreen"
            android:label="@string/title_activity_settings_screen"></activity>
    </application>

</manifest>